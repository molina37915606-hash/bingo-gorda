'use strict';
const assert=require('assert');const{spawn}=require('child_process');const fs=require('fs'),os=require('os'),path=require('path');
const port=54100+Math.floor(Math.random()*200),base=`http://127.0.0.1:${port}`,dataDir=fs.mkdtempSync(path.join(os.tmpdir(),'bingo-alpha6-security-'));
const child=spawn(process.execPath,['server.js'],{cwd:path.join(__dirname,'..'),env:{...process.env,PORT:String(port),BINGO_TEST_MODE:'true',BINGO_DATA_DIR:dataDir,PUBLIC_URL:base},stdio:['ignore','pipe','pipe']});
const wait=ms=>new Promise(r=>setTimeout(r,ms));async function waitServer(){for(let i=0;i<100;i++){try{if((await fetch(base+'/healthz')).ok)return}catch{}await wait(40)}throw Error('server')}
async function req(url,opt={}){const r=await fetch(base+url,opt),d=await r.json().catch(()=>({}));return{r,d}}async function post(url,body,headers={}){const o=await req(url,{method:'POST',headers:{'Content-Type':'application/json',...headers},body:JSON.stringify(body||{})});assert(o.r.ok,`${url} ${o.r.status} ${JSON.stringify(o.d)}`);return o.d}async function get(url,headers={}){const o=await req(url,{headers});return o}
(async()=>{try{await waitServer();const login=await post('/api/admin/login',{}),ah={'X-Admin-Token':login.token};const room=await post('/api/admin/create-simple-room',{mode:90,cardCount:100,autoSeconds:60,rules:{line:true,bingo:true},paymentMode:'free',markingMode:'normal',accessKey:'SEGURA6',maxCardsPerPlayer:2,linePrizeCount:2},ah);
 const ana=await post('/api/player/alpha-join',{accessKey:'SEGURA6',name:'María',cardCount:2,deviceId:'dev-a'}),aH={'X-Player-Token':ana.token};
 let dup=await req('/api/player/alpha-join',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({accessKey:'SEGURA6',name:'maria',cardCount:1,deviceId:'dev-x'})});assert.equal(dup.r.status,400,'María/maria deben ser el mismo nombre');
 const beto=await post('/api/player/alpha-join',{accessKey:'SEGURA6',name:'Beto',cardCount:2,deviceId:'dev-b'}),bH={'X-Player-Token':beto.token};assert.notEqual(ana.token,beto.token);
 let noSession=await get('/api/player/state');assert.equal(noSession.r.status,401,'La clave general no debe dar acceso a ningún jugador');
 assert.equal(Object.prototype.hasOwnProperty.call(ana.state.player,'code'),false,'El jugador no debe recibir un código personal reutilizable.');
 let legacyLogin=await req('/api/player/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:'123456'})});assert.equal(legacyLogin.r.status,410,'El acceso alternativo por código personal debe estar deshabilitado.');
 const aCard=ana.state.player.offeredCards[0];let steal=await req('/api/player/choose',{method:'POST',headers:{'Content-Type':'application/json',...bH},body:JSON.stringify({cardIds:[aCard.id]})});assert.equal(steal.r.status,400,'B no puede apropiarse de un cartón fuera de su sesión/ofertas');
 const aIds=ana.state.player.offeredCards.slice(0,2).map(c=>c.id);await post('/api/player/choose',{cardIds:aIds},aH);let bState=(await get('/api/player/state',bH)).d;assert.equal(bState.player.offeredCards.some(c=>aIds.includes(c.id)),false,'Cartones confirmados de A deben desaparecer para B');
 let foreignMark=await req('/api/player/mark',{method:'POST',headers:{'Content-Type':'application/json',...bH},body:JSON.stringify({cardId:aIds[0],number:Number(ana.state.player.offeredCards[0].grid.flat().find(Number.isFinite)),marked:true})});assert.equal(foreignMark.r.status,400,'Una sesión no puede modificar cartones de otra');
 let st=(await get('/api/admin/state',ah)).d;const mariaAdmin=st.players.find(p=>p.name==='María');const rec=await post('/api/admin/player-recovery-link',{playerId:mariaAdmin.id},ah);const rt=new URL(rec.url).searchParams.get('recuperar');const recovered=await post('/api/player/recover',{recoveryToken:rt,deviceId:'new-device'});assert.notEqual(recovered.token,ana.token,'Recuperar debe rotar la sesión');let old=await get('/api/player/state',aH);assert.equal(old.r.status,401,'La sesión anterior debe quedar invalidada');let reuse=await req('/api/player/recover',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({recoveryToken:rt,deviceId:'again'})});assert.equal(reuse.r.status,400,'El link de recuperación debe usarse una sola vez');
 let queryLeak=await fetch(base+`/jugar?session=${encodeURIComponent(recovered.token)}`,{redirect:'manual'});assert.equal(queryLeak.status,303);assert.equal(queryLeak.headers.get('location'),'/jugador','/jugar no debe aceptar token por URL');
 console.log('PRUEBA ALFA 6 SEGURIDAD: OK · nombre único + sesión privada + cartón exclusivo + recuperación de un solo uso');
}catch(e){console.error(e);process.exitCode=1}finally{child.kill('SIGTERM');fs.rmSync(dataDir,{recursive:true,force:true})}})();
