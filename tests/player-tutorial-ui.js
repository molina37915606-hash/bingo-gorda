const fs = require('fs');
const assert = require('assert');
const html = fs.readFileSync('jugador.html','utf8');
const js = fs.readFileSync('js/online-room-player.js','utf8');

assert(html.includes('id="guideSpotlight"'), 'falta spotlight contextual');
assert(html.includes('id="guideBubble"'), 'falta globo contextual');
assert(html.includes('id="guideSkipBtn"'), 'falta saltar tutorial');
assert(html.includes('id="helpBtn"'), 'falta ? permanente');
assert(html.includes('id="quickAutoMarkBtn"'), 'falta automarcado rapido');
assert(html.includes('id="quickSoundBtn"'), 'falta sonido rapido');
assert(html.includes('id="quickVoiceBtn"'), 'falta voz rapida');
assert(html.includes('id="firstClaimReminder"'), 'falta recordatorio primer reclamo');
assert(html.includes('id="infoDrawerToggle"'), 'la flecha lateral debe mantenerse');
assert(js.includes("target:'#renewChoice'"), 'tutorial no explica recargar cartones');
assert(js.includes("demo:'mark'"), 'tutorial no demuestra marcado manual');
assert(js.includes('Si te dormiste números'), 'tutorial no explica recuperacion por automarcado');
assert(js.includes('Gana el primer reclamo válido'), 'tutorial no explica orden de reclamo');
assert(js.includes("target:'#infoDrawerToggle'"), 'tutorial no explica flecha lateral');
assert(js.includes("target:'#fullScreenBtn'"), 'tutorial no explica pantalla completa');
assert(js.includes("target:'#quickSoundBtn'"), 'tutorial no explica sonido');
assert(js.includes("target:'#quickVoiceBtn'"), 'tutorial no explica voz');
assert(js.includes('renderPregamePreview()'), 'falta vista previa antes del sorteo');
assert(js.includes('autoMarkDesired'), 'automarcado no tiene cola optimista');

assert(js.includes("target:'#selectionPlayerName'"), 'tutorial no explica el nombre en sala de espera');
assert(js.includes("target:'#waitMiniGame'"), 'tutorial no explica minijuegos de espera');
assert(js.includes("target:'.choiceCounter'"), 'tutorial no explica cuantos cartones puede elegir');
assert(js.includes('Después el tutorial sigue en tu pantalla de juego'), 'tutorial no conecta espera con pantalla de juego');
console.log('OK player tutorial UI');
